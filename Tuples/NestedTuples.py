tup=(1,(2,3,4),2)
print(tup[1][2]) #4

#print(tup[0][2]) this will show error because there is not any
#  nested element on index 0 
print(tup[0]) # 0 this will work we can not write like this also 
#  tup[0][0]
print(tup[1])